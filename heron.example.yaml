# Heron Agent Checkpoint — Configuration Example
# Copy this file to heron.yaml and fill in your values

# ─── Scan mode (Heron → Agent) ─────────────────────────────────
target:
  # How to connect to the agent you want to audit
  # Options: http, interactive
  type: http

  # For HTTP targets (OpenAI-compatible chat API)
  url: http://localhost:8080/v1/chat/completions
  # apiKey: sk-...
  # model: gpt-4o  # optional, depends on target

  # For interactive mode (you copy questions to the agent manually):
  # type: interactive

# ─── Server mode (Agent → Heron) ───────────────────────────────
# Start with: npx heron-ai serve
# server:
#   port: 3700
#   host: 0.0.0.0
#   reportDir: ./reports

# ─── LLM for analysis ──────────────────────────────────────────
llm:
  # LLM used by Heron to analyze agent responses.
  # Options: anthropic, openai, gemini
  #
  # First-time setup: just run `heron scan ...` without setting
  # anything here — an interactive wizard will ask which provider
  # to use (Anthropic, OpenAI, Google, OpenRouter, LiteLLM) and
  # collect the credentials it needs.
  provider: anthropic

  # API key resolution (in order):
  #   1. --llm-key flag
  #   2. apiKey below
  #   3. HERON_LLM_API_KEY env var
  #   4. Interactive wizard (TTY only)
  #
  # apiKey: sk-ant-...

  # Optional model override. Defaults track the current flagship
  # per provider — see DEFAULT_MODELS in src/llm/client.ts.
  # model: claude-opus-4-7
  # For OpenAI:  gpt-5.5
  # For Gemini:  gemini-2.5-pro

  # Optional base URL — set this when pointing at an
  # OpenAI-compatible gateway (LiteLLM, OpenRouter, vLLM,
  # Azure-OpenAI proxy). Equivalent to the --llm-base-url CLI flag
  # and the HERON_LLM_BASE_URL env var.
  #   - LiteLLM:    baseURL: https://litellm.example.com
  #   - OpenRouter: baseURL: https://openrouter.ai/api/v1
  # baseURL: https://litellm.example.com

output:
  # Output format: markdown or json
  format: markdown
  # Where to save the report (default: stdout)
  # path: ./reports/audit-report.md
